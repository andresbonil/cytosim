#include "set_python.h"

SetReport::SetReport() {
    //for ( Object * obj=Set->first(); obj; obj=obj->next() )
    //{
    //    reports.push_back(obj->report());
    //}
}

/*  
PyObj::~PyObj() {
    id = 0;
    pos0 = 0;
}
 */
